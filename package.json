{
  "name": "analytics-platform",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "packageManager": "pnpm@11.1.1",
  "engines": {
    "node": ">=26.7.0"
  },
  "scripts": {
    "dev": "turbo run dev --filter=@ap/platform-web",
    "gen:menu": "node tooling/gen-menu/src/cli.ts",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "build": "turbo run build"
  },
  "devDependencies": {
    "turbo": "^2.11.4"
  }
}
