// Existing DESIGN.md alpha chart colors consumed directly by the ECharts SSR adapter (plot.ts).
// UI chrome uses the ported FeedbackOps semantic tokens (styles/tokens.css + styles/theme.css).
export const colors = {
  'chart-blue': '#3b9cff',
  'chart-purple': '#a174f5',
  'chart-grid': '#edf1f7',
} as const;
